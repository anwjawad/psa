# ARCHITECTURE — PSA Eligibility Checklist Web App

## المسار الكامل لطلب واحد

```
Patient Mobile Browser (SPA — Index.html + Styles.html + JavaScript.html)
        │
        │  google.script.run.submitPsaForm(payload)
        ▼
Code.gs — submitPsaForm(payload)
        │
        ├─► validatePayload_(payload)         ← تحقق سيرفري كامل (لا يُوثق بالمتصفح إطلاقًا)
        │       • Region ∈ {North, Central, South}
        │       • Age رقم منطقي (1-120)
        │       • Phone: sanitize لأرقام/+ فقط، لا يُستخدم حسابيًا أبدًا
        │       • كل حقول نعم/لا ∈ {Yes, No} فقط
        │       • الحقول الشرطية (previous_psa_result, chronic.*, ...) تُبنى من جديد سيرفريًا
        │         حسب الإجابات الأخرى — لا يُوثق بأي قيمة شرطية يرسلها المتصفح مباشرة
        │
        ├─► LockService.getScriptLock().tryLock(30s)
        │       │
        │       ▼  (كل ما يلي داخل الـ Lock)
        │   findRowByClientSubmissionId_(sheet, clientId)
        │       │
        │       ├─ إن وُجد → return {success:true, submissionId, duplicate:true}   [لا يُكتب صف جديد]
        │       │
        │       └─ إن لم يوجد →
        │             submission_id  = Utilities.getUuid()
        │             submitted_at   = من ساعة السيرفر (Session.getScriptTimeZone())
        │             age_above_75   = محسوب من Age (لا يُسأل المريض)
        │             sheet.appendRow(row)
        │             SpreadsheetApp.flush()
        │
        └─► lock.releaseLock()  في finally — يُحرَّر دائمًا حتى لو حدث خطأ
        ▼
Google Sheet — PSA_RESPONSES (صف واحد = مريض واحد)
```

## لماذا لا تختلط بيانات المرضى أبدًا (Concurrency)

هذه أهم نقطة في المشروع، وتم اختبارها فعليًا (وليس نظريًا فقط) قبل النشر:

1. **لا يوجد أي Global/Shared State على السيرفر.** لا `currentRow`, لا `currentPatient`. كل استدعاء لـ `submitPsaForm(payload)` مستقل تمامًا ويحمل كل بيانات ذلك المريض في `payload` نفسه — لا اعتماد على أي متغيّر محفوظ بين الطلبات.
2. **`LockService.getScriptLock()`** يضمن أن عمليتين (فحص التكرار + الكتابة) لا يمكن أن تتشابكا بين مريضين مختلفين في نفس اللحظة. الطلب الثاني ينتظر حتى يُحرَّر القفل من الأول قبل أن يبدأ فحصه الخاص.
3. **لا اعتماد على رقم صف يحدده المتصفح.** الكتابة عبر `sheet.appendRow()` فقط — Google Sheets نفسه يحدد مكان الصف الجديد بعد آخر صف مستخدم، والمريض/المتصفح لا يملك أي تأثير على "أين" يُكتب صفه.
4. **الطابع الزمني (`Submitted_At`) يُنشأ من السيرفر حصرًا**، وليس من ساعة هاتف المريض — يمنع أي تلاعب أو تفاوت.

### الإثبات الفعلي (وليس افتراضًا نظريًا)

تم تشغيل اختبارات حقيقية عبر HTTP متزامن فعلي (PowerShell RunspacePool، وليس محاكاة متسلسلة):

| الاختبار | النتيجة |
|---|---|
| 20 طلبًا متزامنًا حقيقيًا (مرضى مختلفون) | 20/20 نجحوا، 20 `Submission_ID` فريد، لا تداخل |
| 50 طلبًا متزامنًا حقيقيًا | 50/50 نجحوا، 50 `Submission_ID` فريد، لا تداخل |
| 15 طلبًا متزامنًا **بنفس** `Client_Submission_ID` (محاكاة Race Condition حقيقي) | استجابة واحدة فقط بـ `duplicate:false`، والـ 14 الباقية `duplicate:true` بنفس الـ `Submission_ID` — **صف واحد فقط كُتب في الشيت** |
| عدد صفوف الشيت بعد كل الاختبارات (76 استدعاء ناجح متوقع) | 76 صفًا بالضبط — مطابق تمامًا (لا نقص، لا زيادة، لا تكرار) |

## الحماية من الإرسال المزدوج (Idempotency)

طبقتان مستقلتان:

1. **طبقة العميل (UX):** فور الضغط على "إرسال الاستمارة"، الزر يُعطَّل فورًا ويظهر Spinner، ومتغيّر `submitInFlight` يمنع أي محاولة ثانية من نفس الجلسة قبل عودة الرد.
2. **طبقة السيرفر (الحقيقية/الملزمة):** `clientSubmissionId` (UUID يُنشأ مرة واحدة على جهاز المريض عند الوصول لشاشة المراجعة) يُرسَل مع كل محاولة. قبل أي `appendRow`، يبحث السيرفر — **داخل الـ Lock** — عن نفس المعرّف في عمود `Client_Submission_ID`. إن وُجد، يُعاد نفس `Submission_ID` القديم بدل إنشاء صف جديد. هذه الطبقة هي التي تحمي فعليًا من انقطاع الإنترنت/إعادة المحاولة/الضغط المزدوج — ولو تم تجاوز طبقة العميل بأي شكل.

## الأمان

- `SPREADSHEET_ID` و`SHEET_NAME` محفوظان في **Script Properties** فقط — لا يظهران أبدًا في أي HTML/JS يصل للمتصفح.
- المتصفح لا يتحدث مع Google Sheets مباشرة تحت أي ظرف — المسار الوحيد هو `google.script.run` إلى دوال محددة سيرفريًا.
- أخطاء التحقق تُعاد للمريض برسائل عربية عامة وآمنة؛ تفاصيل الأخطاء التقنية (`err.stack`) تُسجَّل فقط سيرفريًا عبر `console.error`، ولا تصل للمتصفح أبدًا.
- لا console.log لبيانات مرضى حساسة.
- الحقول الحساسة (نتيجة PSA، التشخيصات) لا تُعرض مجددًا في شاشة النجاح.

## نقاط تصميم شُرحت للمستخدم صراحة (وليست تغييرًا صامتًا في المحتوى الطبي)

- **العمر > 75 سنة** (من جدول الاستبعاد في المصدر): يُحسب تلقائيًا من حقل "العمر" الذي يُجمع أصلًا، بدل تكراره كسؤال منفصل. القيمة تُخزَّن في عمود `Age_Above_75`.
- **"هل تتابع حاليًا مع طبيب مختص؟"** (من جدول الاستبعاد): يظهر فقط إذا أجاب المريض أن لديه تشخيصًا سابقًا بسرطان البروستاتا (إجابة "لا" على السؤال التاسع الأصلي)، بدل عرضه دائمًا كسؤال ثابت غير ذي صلة لبقية المرضى.

كلا القرارين لا يحذفان أي معلومة من الاستمارة الأصلية — فقط يغيّران **متى/كيف** تُجمع، لتقليل عدد الأسئلة المعروضة فعليًا وتقصير الوقت اللازم للإنجاز.
